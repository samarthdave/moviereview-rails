<div class="container">
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name),
    :html => {class: 'well well-transparent col-md-4 col-md-offset-3'}) do |f| %>
    <h2>Sign up</h2>
    <%= devise_error_messages! %>
  
    <div class="form-group">
      <%= f.label :email, class: 'sr-only' %>
      <%= f.email_field :email, class: 'form-control movies-input',
        placeholder: 'Email' %>
    </div>
  
    <div class="form-group">
      <%= f.label :password, class: 'sr-only' %>
      <%= f.password_field :password, autocomplete: "off", class: 'form-control movies-input',
         placeholder: "Password#{" (#{@minimum_password_length} characters minimum)" if @minimum_password_length}" %>
    </div>
  
    <div class="form-group">
      <%= f.label :password_confirmation, class: 'sr-only' %>
      <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control movies-input',
         placeholder: 'Confirm password' %>
    </div>
  
    <div class="form-group">
      <%= f.submit "Sign up", class: 'btn btn-lp' %>
      <%= render "devise/shared/links" %>
    </div>
  <% end %>
</div>